# Kitchen Service 👨‍🍳

Сервис "Кухня" (Worker). Имитирует процесс приготовления заказа. Это классический микросервис-воркер, который не имеет API для внешних клиентов, а только слушает очередь сообщений.
В качестве полезной нагрузки использует `asyncio.sleep` для имитации задержки.

## Технологии

- **FastAPI**: Используется как основа приложения.
- **FastStream[rabbit]**: Работа с RabbitMQ (Publisher и Subscriber). Декораторы `@router.subscriber` и `@router.publisher` управляют потоком сообщений.

## Структура

- `app.py`: Точка входа.
- `api.py`: Основная логика обработки сообщений.
  - Слушает очередь `kitchen_queue` (событие `order.created`).
  - Высчитывает время приготовления заказа.
  - Отправляет `order.started` через 1-15 сек (имитация подтверждения).
  - "Готовит" заказ за вычисленное время (имитация задержки).
  - Отправляет `order.processed` после окончания "готовки".

Из `shared` библиотеки используются:

- `shared.config`: Конфигурация подключения к брокеру.
- `shared.schemas`: Схемы для сериализации данных.
- `shared.topics`: Топики для общения между сервисами.
- `shared.logger`: функция `setup_logger` для настройки логирования.

## Взаимодействие

1.  **Input (RabbitMQ)**: Слушает `order.created` в очереди `kitchen_queue`.
2.  **Process**: Имитирует задержку (время принятия заказа кухней + время приготовления).
3.  **Output (RabbitMQ)**: Публикует `order.started` -> `order.processed` для обновления статуса.
