# Cashier Service 💵

Сервис "Касса", который отвечает за прием HTTP-запросов от клиентов (в проекте -- от бота) и создание событий в брокере сообщений.

## Технологии

- **FastAPI**: REST API для приема заказов.
- **FastStream[rabbit]**: Работа с RabbitMQ (Publisher). Функция `broker.publish` используется для отправки сообщений в брокер.

## Структура

- `app.py`: Точка входа, `lifespan` для подключения к брокеру.
- `api/order.py`: Эндпоинт `POST /api/order`. Принимает заказ и публикует событие `order.created`.
- `api/item.py`: Эндпоинт `GET /api/items`. Отдает меню из файла JSON.
- `core/broker.py`: Настройки подключения к RabbitMQ.

Из `shared` библиотеки используются:

- `shared.config`: Конфигурация подключения к брокеру.
- `shared.schemas`: Схемы для сериализации данных.
- `shared.topics`: Топики для общения между сервисами.
- `shared.logger`: функция `setup_logger` для настройки логирования.

## Взаимодействие

1.  **Input (HTTP)**: Получает POST запрос с данными заказа.
2.  **Process**: Валидирует данные.
3.  **Output (RabbitMQ)**: Публикует сообщение в exchange `orders` с routing key `order.created`.
